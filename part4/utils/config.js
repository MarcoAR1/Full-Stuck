const PORT = process.env.PORT
const MONGODB_URI = process.env.MONGODB_URI
const TEST_MONGODB_URI = process.env.TEST_MONGODB_URI
const NODE_ENV = process.env.NODE_ENV
const SING = process.env.SING
module.exports = {
  PORT,
  MONGODB_URI,
  TEST_MONGODB_URI,
  NODE_ENV,
  SING,
}
